/**
 * The GlimStone release the ports in this folder follow, copied from the
 * design language's reference/react/version.ts. It ships beside the files it
 * describes so the About card cannot drift from them; bump it in the change
 * that lifts them. The number is a link, so it has to name a published release.
 */
export const GLIMSTONE_VERSION = '2.13.0';
